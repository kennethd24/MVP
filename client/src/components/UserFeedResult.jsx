import React from 'react';

const userFeedResult = (props) => {
  const { newUserFeed } = props;

  return (
    <div>
      results table of last 100 videos
    </div>
  );
};
export default userFeedResult;
